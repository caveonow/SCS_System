<%= form_for(@background) do |f| %>
  <% if @background.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@background.errors.count, "error") %> prohibited this background from being saved:</h2>

      <ul>
      <% @background.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :BackgroundName %><br>
    <%= f.text_field :BackgroundName %>
  </div>
  <div class="field">
    <%= f.label :BackgroundPic %><br>
    <%= f.text_field :BackgroundPic %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
