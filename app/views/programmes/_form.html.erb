<%= form_for(@programme) do |f| %>
  <% if @programme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@programme.errors.count, "error") %> prohibited this programme from being saved:</h2>

      <ul>
      <% @programme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :programmename %><br>
    <%= f.text_field :programmename %>
  </div>
  
  <div class="field">
		    <%= f.label :faculty_id %><br>
		    <%= collection_select(:programme, :faculty_id, Faculty.all, :id, :facultyname, {prompt: true}) %>	    
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
