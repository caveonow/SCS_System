<%= form_for(@advertisement) do |f| %>
  <% if @advertisement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advertisement.errors.count, "error") %> prohibited this advertisement from being saved:</h2>

      <ul>
      <% @advertisement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class="row">
	<div class="large-6 large-centered columns">
	<div class="row column log-in-form">

	  <div class="field field-padding">
	    <%= f.label :AdvertisementName, "Title" %><br>
	    <%= f.text_field :AdvertisementName%>
	  </div>
	  <div class="field field-padding">
	    <%= f.label :AdvertisementDescription , "Description"  %><br>
	    <%= f.text_area :AdvertisementDescription %>
	  </div>  
	  <div class="field field-padding">
	    <%= f.label :imageAd, "Image file, only accept jpg, jpeg, gif and png files" %><br />
	    <%= f.file_field :imageAd %>
	  </div>
	  <div class="actions field-padding">
	    <%= f.submit %>
	  </div>	  
	</div>
	</div>
	</div>
<% end %>

